package dates

/*

var (
	Calendar = make([]byte, int(DAYS_YEAR))
)



func addDays(Calendar *[]float64, count float64, range1, range2 int) {
	for i := range (*Calendar)[range1:range2] {
		ls := (*Calendar)[range1:range2]

		ls[i] += count
	}
}
*/
